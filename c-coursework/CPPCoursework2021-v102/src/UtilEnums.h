#pragma once

enum class EnemyType {
    SLIME,
    FROG,
    BIG_FROG,
    GOOBER
};

enum player_states {
    WALK,
    RUN,
    ATTACK
};

enum class Directions {
    LEFT,
    RIGHT,
    UP,
    DOWN
};

enum class FGDetail_type {
    CAMP,
    ROCK,
    TREE,
    HOUSE
};

enum class BGDetail_Type {
    TREE,
    ROCK
};
